# ComfyUI MCP Server Environment Configuration
# Copy this file to .env and customize the values as needed

# =============================================================================
# ComfyUI Server Settings
# =============================================================================
# These settings control the connection to your ComfyUI server instance

# ComfyUI server host address
# Default: localhost
# Examples: localhost, 192.168.1.100, comfyui.example.com
COMFYUI_HOST=localhost

# ComfyUI server port
# Default: 8188
COMFYUI_PORT=8188

# ComfyUI authentication token (optional)
# Leave empty if your ComfyUI server doesn't require authentication
# Format: Bearer <token> or just <token>
# Default: (empty)
# COMFYUI_AUTHENTICATION=Bearer your_token_here

# Directory containing workflow JSON files
# Default: workflows
# This should be relative to the project root or an absolute path
COMFYUI_WORKFLOW_DIR=workflows

# Subfolder for uploaded images in ComfyUI
# Default: uploads
# Images uploaded via the edit_image tool will be placed in this subfolder
COMFYUI_UPLOAD_SUBFOLDER=uploads

# =============================================================================
# MCP Server Settings
# =============================================================================
# These settings control the MCP server itself

# MCP server host address
# Default: 0.0.0.0 (listens on all interfaces)
# Use 127.0.0.1 to only listen on localhost
MCP_HOST=0.0.0.0

# MCP server port
# Default: 8000
MCP_PORT=8000

# MCP transport type
# Options: stdio, sse, http
# Default: stdio
# - stdio: Standard input/output (for direct MCP client connections)
# - sse: Server-Sent Events (for web-based clients)
# - http: HTTP transport (for REST API clients)
MCP_TRANSPORT=stdio

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
MCP_LOG_LEVEL=INFO

# Log file path (optional)
# If specified, logs will be written to this file in addition to console
# Default: (empty - logs only to console)
# Example: app.log, logs/app.log, /var/log/comfyui-mcp.log
# MCP_LOG_FILE=

# Log format (optional)
# Python logging format string
# Default: %(asctime)s - %(name)s - %(levelname)s - %(message)s
# MCP_LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

